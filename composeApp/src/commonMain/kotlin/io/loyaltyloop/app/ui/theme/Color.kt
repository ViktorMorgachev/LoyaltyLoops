package io.loyaltyloop.app.ui.theme

import androidx.compose.ui.graphics.Color

// --- Основная палитра (Brand Colors) ---
val LoyaltyIndigo = Color(0xFF4F46E5) // Основной (Primary)
val LoyaltyIndigoDark = Color(0xFF3730A3) // Для темной темы

val LoyaltyEmerald = Color(0xFF10B981) // Акцент/Успех (Secondary)
val LoyaltyEmeraldDark = Color(0xFF059669)

val LoyaltyCoral = Color(0xFFEF4444) // Ошибка (Error)

// --- Нейтральные цвета (Backgrounds & Text) ---
val White = Color(0xFFFFFFFF)
val Gray50 = Color(0xFFF9FAFB) // Фон приложения (светлый)
val Gray100 = Color(0xFFF3F4F6) // Фон полей ввода
val Gray300 = Color(0xFFD1D5DB) // Границы
val Gray500 = Color(0xFF6B7280) // Второстепенный текст
val Gray900 = Color(0xFF111827) // Основной текст (почти черный)

// Цвета для Темной темы (Dark Mode)
val DarkBackground = Color(0xFF111827)
val DarkSurface = Color(0xFF1F2937)
val DarkTextPrimary = Color(0xFFF9FAFB)
val DarkTextSecondary = Color(0xFF9CA3AF)